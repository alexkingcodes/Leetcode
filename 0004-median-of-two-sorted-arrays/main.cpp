#include "solution.h"
#include <iostream>

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    // TODO: read input and call Solution methods
    Solution sol;
    (void)sol;

    return 0;
}
